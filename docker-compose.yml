nginx:
  container_name: nginx
  build: docker/nginx
  volumes:
    - ".:/var/www/html"
  ports:
    - "8080:80"
  links:
    - fpm:fpm
    - redis:redis

fpm:
  container_name: fpm
  build: docker/php7-fpm
  volumes:
    - ".:/var/www/html"

redis:
  container_name: redis
  build: docker/redis